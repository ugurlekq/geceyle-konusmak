'use client';
import { useEffect, useState } from 'react';
type Theme = 'dim' | 'amber' | 'mono';

'use client';

export default function ThemeToggle() {
    return null; // ← derleme hatasını çözer, hiçbir görsel üretmez
}

